page { padding: 16px; width: 100%; }

.stats { display: flex; gap: 8px; margin-bottom: 16px; }
.stat-card { flex: 1; background: white; border-radius: 8px; padding: 12px; text-align: center; }
.num { font-size: 24px; font-weight: 700; display: block; }
.lbl { font-size: 12px; color: #888; }
.stat-card.safe .num { color: #27ae60; }
.stat-card.warn .num { color: #f39c12; }
.stat-card.expired .num { color: #e74c3c; }

.toolbar { display: flex; gap: 8px; margin-bottom: 12px; align-items: center; }
.toolbar input { flex: 1; height: 40px; padding: 0 10px; border: 1px solid #ddd; border-radius: 8px; font-size: 14px; background: white; min-width: 0; line-height: 40px; }
.export-btn { background: #4472C4 !important; color: white !important; border-radius: 6px !important; font-size: 13px !important; white-space: nowrap; flex-shrink: 0; }

.list { }
.item-card { background: white; border-radius: 8px; padding: 14px; margin-bottom: 10px; display: flex; border-left: 4px solid; position: relative; overflow: hidden; }
.item-card.safe { border-left-color: #27ae60; }
.item-card.warn { border-left-color: #f39c12; }
.item-card.expired { border-left-color: #e74c3c; }
.card-left { flex: 1; overflow: hidden; min-width: 0; }
.card-right { text-align: right; display: flex; flex-direction: column; align-items: flex-end; flex-shrink: 0; padding-right: 20px; }
.item-name { font-size: 16px; font-weight: 600; display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.item-brand { font-size: 13px; color: #888; display: block; margin-top: 2px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.item-dates { margin-top: 8px; }
.date-row { display: flex; align-items: center; margin-bottom: 4px; }
.dl { font-size: 12px; color: #888; min-width: 56px; }
.dv { font-size: 13px; color: #555; }
.remaining { font-size: 20px; font-weight: 700; }
.unit { font-size: 13px; color: #888; font-weight: 400; }
.remaining.safe { color: #27ae60; }
.remaining.warn { color: #f39c12; }
.remaining.expired { color: #e74c3c; }
.badge { font-size: 11px; padding: 2px 8px; border-radius: 10px; margin-top: 4px; }
.badge.safe { background: #e8f5e9; color: #27ae60; }
.badge.warn { background: #fff8e1; color: #f39c12; }
.badge.expired { background: #ffebee; color: #e74c3c; }
.delete-btn { position: absolute; bottom: 14px; right: 14px; color: #ccc; font-size: 18px; padding: 4px; }

.empty { text-align: center; padding: 40px; color: #999; font-size: 14px; }
.empty .sub { display: block; margin-top: 8px; font-size: 13px; }
