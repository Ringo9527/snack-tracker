page { background: #f5f5f5; box-sizing: border-box; overflow-x: hidden; }
view, text, input, button, picker, image { box-sizing: border-box; }
.status-safe { color: #27ae60; }
.status-warn { color: #f39c12; }
.status-expired { color: #e74c3c; }
